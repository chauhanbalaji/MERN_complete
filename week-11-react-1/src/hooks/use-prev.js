// import { useEffect, useRef } from "react"

// export const usePrev = (value) => {
//   const ref = useRef();
//   // it Reference
//   // it store the previous value and it give the vlaue 
  
//   console.log("re-render happened with new value " + value);
  
//   useEffect(() => {
//     console.log("updated the ref to be " + value);
//     ref.current = value;
//     // it store the updat the value as current value 

//   }, [value]);
  
//   console.log("returned " + ref.current);
//   // it return the value
//   return ref.current;
    
// }


